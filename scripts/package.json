{
  "name": "ntumagic-scripts",
  "private": true,
  "description": "本地腳本：seed 座位、一次性 maintenance 用",
  "scripts": {
    "seed": "node seed-seats.js",
    "seed:reset": "node seed-seats.js --reset"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0"
  }
}
